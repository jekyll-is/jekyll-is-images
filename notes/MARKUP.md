# Разметка и атрибуты

Мы используем стандартную разметку Markdown, переинтерпретируя некоторые элементы и атрибуты.

Через стандартный механизм атрибутов в `{: ... }` мы передаем все параметры как изображений, так и блоков `<figure>`.
